# Frontend environment configuration (OPTIONAL).
# With the Vite dev server running on :5173, /api and /health are proxied to
# the backend automatically - no frontend env file is required.

# Set this only when the backend is NOT reachable through the dev proxy:
# VITE_API_BASE_URL=http://localhost:8000